// requirejs的define
define(['angular'], function () {
    // 建立module，並引入其他module
    var app = angular.module('app', ['xModule']);

    // module設定
    app.config(function () {

    });

    // module執行期設定
    app.run(function () {

    });
});